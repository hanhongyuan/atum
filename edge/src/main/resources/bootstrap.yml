spring:
  #应用名
  application:
    name: edge
  #云配置
  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: admin

---

spring:
  profiles: dev
  #应用名
  application:
    name: edge
  #云配置
  cloud:
    config:
      uri: http://wxtest.oasisapp.cn:8888
      username: admin
      password: admin