spring:
  #应用名
  application:
    name: wechat
  #云配置
  cloud:
    config:
      uri: http://localhost:8888
      username: admin
      password: admin

---

spring:
  profiles: dev
  #应用名
  application:
    name: wechat
  #云配置
  cloud:
    config:
      uri: http://wxtest.oasisapp.cn:8888
      username: admin
      password: admin

---

spring:
  profiles: prod
  #应用名
  application:
    name: wechat
  #云配置
  cloud:
    config:
      uri: http://wechat.oasiscare.cn:8888
      username: admin
      password: admin

---

spring:
  profiles: docker
  #应用名
  application:
    name: wechat
  #云配置
  cloud:
    config:
      uri: ${CONFIG_SERVER}
      username: admin
      password: admin
